<%- include('partials/header', { title: "Profile Page" }) %>

<div class="main-content">
    <h1>Profile Page</h1>
    <% if (errorMessage) { %>
        <p class="error-message"><%= errorMessage %></p>
    <% } %>
    <h2>Welcome, <%= user.username %></h2>
    <h2>You have voted in a whopping: <%= pollVoteCount %> polls!</h2>
</div>

</body>
</html>